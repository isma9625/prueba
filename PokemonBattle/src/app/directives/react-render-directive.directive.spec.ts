import { ReactRenderDirectiveDirective } from './react-render-directive.directive';

describe('ReactRenderDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new ReactRenderDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
